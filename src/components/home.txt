 <div className="flex-1 flex flex-col space-y-8">
        {/* Applicants List */}
        <div className="flex gap-6 border-b pb-6 relative">
          {applicants.map((applicant, index) => (
            <div 
              key={index} 
              className="flex flex-col items-center gap-3 relative cursor-pointer group"
              onClick={() => setSelectedApplicant(applicant)}
            >
              <div className="flex items-center gap-3">
                <span className={`text-lg ${selectedApplicant === applicant ? 'text-[#4785FF]' : 'text-gray-600'}`}>
                  {applicant}
                </span>
                <Button 
                  variant="ghost" 
                  size="icon" 
                  className="h-12 w-12 bg-[#4785FF] hover:bg-blue-600 p-2"
                >
                  <Trash2 className="h-6 w-6 text-white" />
                </Button>
              </div>
              <AnimatePresence>
                {selectedApplicant === applicant && (
                  <motion.div 
                    className="absolute -bottom-[24px] left-0 w-full h-0.5 bg-[#4785FF]"
                    initial={{ scaleX: 0, opacity: 0 }}
                    animate={{ scaleX: 1, opacity: 1 }}
                    exit={{ scaleX: 0, opacity: 0 }}
                    transition={{ 
                      type: "spring",
                      stiffness: 260,
                      damping: 20
                    }}
                    style={{ originX: 0 }}
                  />
                )}
              </AnimatePresence>
            </div>
          ))}
        </div>

        {/* Document Section */}
        <div className="flex gap-6">
          <div className="flex flex-col gap-4">
            <Button className="bg-[#4785FF] hover:bg-blue-600 h-14 w-28 text-base font-normal">
              addhar
            </Button>
            <Button className="bg-[#4785FF] hover:bg-blue-600 h-14 w-28 text-base font-normal flex items-center justify-center">
              + Add
            </Button>
          </div>

          <Card className="flex-1 p-8">
            <div className="flex gap-4 mb-8">
              <Button className="bg-[#4785FF] hover:bg-blue-600 px-6 py-3 text-base">
                + Choose
              </Button>
              <Button 
                variant="secondary"
                className="bg-blue-100 hover:bg-blue-200 text-[#4785FF] px-6 py-3 text-base"
              >
                ↑ Upload
              </Button>
              <Button 
                variant="secondary"
                className="bg-blue-100 hover:bg-blue-200 text-[#4785FF] px-6 py-3 text-base"
              >
                × Cancel
              </Button>
            </div>

            <div
              onDrop={handleDrop}
              onDragOver={handleDragOver}
              className="border-2 border-dashed rounded-lg p-8 text-center text-gray-500"
            >
              Drag and Drop files here.
            </div>
          </Card>
        </div>
      </div>